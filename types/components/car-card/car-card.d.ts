import { LitElement } from 'lit';
import type { Car } from '../../types';
export declare class CarCards extends LitElement {
    car: Car;
    static styles: import("lit").CSSResult;
    protected render(): unknown;
}
